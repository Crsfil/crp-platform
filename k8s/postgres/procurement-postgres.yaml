apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: procurement-postgres
  namespace: crp
spec:
  serviceName: procurement-postgres
  replicas: 1
  selector: { matchLabels: { app: procurement-postgres } }
  template:
    metadata: { labels: { app: procurement-postgres } }
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          env:
            - { name: POSTGRES_DB, value: procurementdb }
            - name: POSTGRES_USER
              valueFrom: { secretKeyRef: { name: procurement-db, key: username } }
            - name: POSTGRES_PASSWORD
              valueFrom: { secretKeyRef: { name: procurement-db, key: password } }
          ports:
            - containerPort: 5432
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
    - metadata: { name: data }
      spec:
        accessModes: [ ReadWriteOnce ]
        resources: { requests: { storage: 5Gi } }
---
apiVersion: v1
kind: Service
metadata:
  name: procurement-postgres
  namespace: crp
spec:
  clusterIP: None
  selector: { app: procurement-postgres }
  ports:
    - name: db
      port: 5432
      targetPort: 5432
