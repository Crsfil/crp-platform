apiVersion: apps/v1
kind: Deployment
metadata:
  name: kyc-service
  namespace: crp
spec:
  replicas: 1
  selector: { matchLabels: { app: kyc-service } }
  template:
    metadata: { labels: { app: kyc-service } }
    spec:
      containers:
        - name: kyc-service
          image: ghcr.io/your-org/kyc-service:latest
          ports: [ { containerPort: 8086 } ]
          livenessProbe:
            httpGet: { path: /actuator/health, port: 8086 }
            initialDelaySeconds: 30
            periodSeconds: 15
          readinessProbe:
            httpGet: { path: /actuator/health, port: 8086 }
            initialDelaySeconds: 10
            periodSeconds: 10
          resources:
            requests: { cpu: "100m", memory: "256Mi" }
            limits: { cpu: "500m", memory: "512Mi" }
          env:
            - { name: SERVER_PORT, value: "8086" }
            - name: INTERNAL_API_KEY
              valueFrom: { secretKeyRef: { name: crp-shared, key: internal-api-key } }
---
apiVersion: v1
kind: Service
metadata:
  name: kyc-service
  namespace: crp
spec:
  selector: { app: kyc-service }
  ports:
    - name: http
      port: 8086
      targetPort: 8086
