apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory-service
  namespace: crp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: inventory-service
  template:
    metadata:
      labels:
        app: inventory-service
    spec:
      containers:
        - name: inventory-service
          image: ghcr.io/your-org/inventory-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8082
          env:
            - name: SERVER_PORT
              value: "8082"
            - name: DB_URL
              value: jdbc:postgresql://inventory-postgres.crp.svc.cluster.local:5432/inventorydb
            - name: DB_USERNAME
              value: inventory
            - name: DB_PASSWORD
              value: inventory
            - name: KAFKA_BOOTSTRAP
              value: kafka:9092
            - name: REDIS_HOST
              value: redis
            - name: REDIS_PORT
              value: "6379"
            - name: SECURITY_JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: crp-shared
                  key: jwt-secret
            - name: INTERNAL_API_KEY
              valueFrom:
                secretKeyRef:
                  name: crp-shared
                  key: internal-api-key
---
apiVersion: v1
kind: Service
metadata:
  name: inventory-service
  namespace: crp
spec:
  selector:
    app: inventory-service
  ports:
    - name: http
      port: 8082
      targetPort: 8082
