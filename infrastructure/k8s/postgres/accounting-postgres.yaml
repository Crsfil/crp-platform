apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: accounting-postgres
  namespace: crp
spec:
  serviceName: accounting-postgres
  replicas: 1
  selector: { matchLabels: { app: accounting-postgres } }
  template:
    metadata: { labels: { app: accounting-postgres } }
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          env:
            - { name: POSTGRES_DB, value: accountingdb }
            - name: POSTGRES_USER
              valueFrom: { secretKeyRef: { name: accounting-db, key: username } }
            - name: POSTGRES_PASSWORD
              valueFrom: { secretKeyRef: { name: accounting-db, key: password } }
          ports:
            - containerPort: 5432
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
    - metadata: { name: data }
      spec:
        accessModes: [ ReadWriteOnce ]
        resources: { requests: { storage: 5Gi } }
---
apiVersion: v1
kind: Service
metadata:
  name: accounting-postgres
  namespace: crp
spec:
  clusterIP: None
  selector: { app: accounting-postgres }
  ports:
    - name: db
      port: 5432
      targetPort: 5432
