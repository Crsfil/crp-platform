apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: application-postgres
  namespace: crp
spec:
  serviceName: application-postgres
  replicas: 1
  selector: { matchLabels: { app: application-postgres } }
  template:
    metadata: { labels: { app: application-postgres } }
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          env:
            - { name: POSTGRES_DB, value: applicationdb }
            - name: POSTGRES_USER
              valueFrom: { secretKeyRef: { name: application-db, key: username } }
            - name: POSTGRES_PASSWORD
              valueFrom: { secretKeyRef: { name: application-db, key: password } }
          ports:
            - containerPort: 5432
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
    - metadata: { name: data }
      spec:
        accessModes: [ ReadWriteOnce ]
        resources: { requests: { storage: 5Gi } }
---
apiVersion: v1
kind: Service
metadata:
  name: application-postgres
  namespace: crp
spec:
  clusterIP: None
  selector: { app: application-postgres }
  ports:
    - name: db
      port: 5432
      targetPort: 5432
