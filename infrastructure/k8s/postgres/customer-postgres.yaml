apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: customer-postgres
  namespace: crp
spec:
  serviceName: customer-postgres
  replicas: 1
  selector: { matchLabels: { app: customer-postgres } }
  template:
    metadata: { labels: { app: customer-postgres } }
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          env:
            - { name: POSTGRES_DB, value: customerdb }
            - name: POSTGRES_USER
              valueFrom: { secretKeyRef: { name: customer-db, key: username } }
            - name: POSTGRES_PASSWORD
              valueFrom: { secretKeyRef: { name: customer-db, key: password } }
          ports:
            - containerPort: 5432
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
    - metadata: { name: data }
      spec:
        accessModes: [ ReadWriteOnce ]
        resources: { requests: { storage: 5Gi } }
---
apiVersion: v1
kind: Service
metadata:
  name: customer-postgres
  namespace: crp
spec:
  clusterIP: None
  selector: { app: customer-postgres }
  ports:
    - name: db
      port: 5432
      targetPort: 5432
