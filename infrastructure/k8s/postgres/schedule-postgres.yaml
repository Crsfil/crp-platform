apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: schedule-postgres
  namespace: crp
spec:
  serviceName: schedule-postgres
  replicas: 1
  selector: { matchLabels: { app: schedule-postgres } }
  template:
    metadata: { labels: { app: schedule-postgres } }
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          env:
            - { name: POSTGRES_DB, value: scheduledb }
            - name: POSTGRES_USER
              valueFrom: { secretKeyRef: { name: schedule-db, key: username } }
            - name: POSTGRES_PASSWORD
              valueFrom: { secretKeyRef: { name: schedule-db, key: password } }
          ports:
            - containerPort: 5432
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
    - metadata: { name: data }
      spec:
        accessModes: [ ReadWriteOnce ]
        resources: { requests: { storage: 5Gi } }
---
apiVersion: v1
kind: Service
metadata:
  name: schedule-postgres
  namespace: crp
spec:
  clusterIP: None
  selector: { app: schedule-postgres }
  ports:
    - name: db
      port: 5432
      targetPort: 5432
