apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: inventory-postgres
  namespace: crp
spec:
  serviceName: inventory-postgres
  replicas: 1
  selector: { matchLabels: { app: inventory-postgres } }
  template:
    metadata: { labels: { app: inventory-postgres } }
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          env:
            - { name: POSTGRES_DB, value: inventorydb }
            - name: POSTGRES_USER
              valueFrom: { secretKeyRef: { name: inventory-db, key: username } }
            - name: POSTGRES_PASSWORD
              valueFrom: { secretKeyRef: { name: inventory-db, key: password } }
          ports:
            - containerPort: 5432
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
    - metadata: { name: data }
      spec:
        accessModes: [ ReadWriteOnce ]
        resources: { requests: { storage: 5Gi } }
---
apiVersion: v1
kind: Service
metadata:
  name: inventory-postgres
  namespace: crp
spec:
  clusterIP: None
  selector: { app: inventory-postgres }
  ports:
    - name: db
      port: 5432
      targetPort: 5432
