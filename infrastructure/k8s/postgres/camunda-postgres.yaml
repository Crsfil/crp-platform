apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: camunda-postgres
  namespace: crp
spec:
  serviceName: camunda-postgres
  replicas: 1
  selector: { matchLabels: { app: camunda-postgres } }
  template:
    metadata: { labels: { app: camunda-postgres } }
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          env:
            - { name: POSTGRES_DB, value: camunda }
            - name: POSTGRES_USER
              valueFrom: { secretKeyRef: { name: camunda-db, key: username } }
            - name: POSTGRES_PASSWORD
              valueFrom: { secretKeyRef: { name: camunda-db, key: password } }
          ports:
            - containerPort: 5432
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
    - metadata: { name: data }
      spec:
        accessModes: [ ReadWriteOnce ]
        resources: { requests: { storage: 5Gi } }
---
apiVersion: v1
kind: Service
metadata:
  name: camunda-postgres
  namespace: crp
spec:
  clusterIP: None
  selector: { app: camunda-postgres }
  ports:
    - name: db
      port: 5432
      targetPort: 5432
